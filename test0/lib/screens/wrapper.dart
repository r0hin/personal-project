import 'package:flutter/material.dart';
import 'package:test0/screens/auth/authentication.dart';

class Wrapper extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Authenticate();
  }
}
